package class_5.finalUsing;

//public final class Point
/*
public class Point {
    // private final int MAX_RANGE = 1 000;
    final StringBuilder builder = new StringBuilder();
    private int x;

    public  Point(){
        this.x = x;
        builder.append("hello");
    }
    // final method
    final int length(){//final этого метода указывает, что в подклассе переопределить метод будет невозможно --> на запрет переопределениe
        //public final class and public final method писать безсмысленно (двойной запрет выполнить одно и тоже действие)

        return Math.abs(x);
    }

    public void setLocation(int x, int y) {
    }

    public int getX() {
        return 0;
    }
    @Test
    public void givenParallelLines_whenCalculatePoint_thenEmpty() {
        int m1 = 1;
        int b1 = 0;
        double m2 = 1;
        int b2 = -1;

        LineIntersection service = null;
        Optional<Point> point = service.calculateIntersectionPoint(m1, b1, (int) m2, b2);

        assertFalse(point.isPresent());
    }

    private void assertFalse(boolean present) {
    }

    public int getY() {
        return 0;
    }
}
*/
